#include "main.h"
/**
 * print_numbers - a function that print out numbers
 * main - encloses code
 *
 * Return: 0 when successful
*/

int main(void)
{
	void print_numbers(void);
	return (0);
}

void print_numbers(void)
{
	int num = 0
		while (num < 10)
		{
			_putchar(num);
			num++
		}
	return (0);
}
