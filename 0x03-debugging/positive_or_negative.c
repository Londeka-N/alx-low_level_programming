#include "main.h"
/**
 * positive_or_negative - print positive or negative digit
 *
 * @i: number seen onoutput
 *
 * Retun: 0 for now
*/

void positive_or_negative(int i)
{
	if (i < 0)
		printf("%d is negative", i);
	else
		printf("%d is positive", i);
}
